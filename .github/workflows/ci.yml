name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel superseded runs on the same branch/PR instead of letting them
# queue up and finish out of order.
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci

      # @prisma/client's own postinstall already runs this, but pinning it
      # explicitly guards against that being skipped by a future npm/CI
      # config change (e.g. --ignore-scripts) — schema.prisma types must
      # exist before typecheck/build can even resolve @prisma/client.
      - run: npx prisma generate

      - name: Typecheck
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      # Unit suite only — vitest.config.ts excludes *.integration.test.ts, so
      # nothing here touches Postgres and this step needs no database. The
      # integration job below covers the DB-backed tests.
      - name: Unit tests
        run: npm run test

      # No live database is reached during `next build` — every DB-backed
      # route in this app is force-dynamic (cookie-based session checks),
      # so nothing queries at build time. These are placeholder values in
      # the shape better-auth/Prisma expect, not real secrets.
      - name: Build
        run: npm run build
        env:
          DATABASE_URL: "postgresql://ci:ci@localhost:5432/ci_placeholder"
          BETTER_AUTH_SECRET: "ci-placeholder-secret-not-used-at-runtime"
          BETTER_AUTH_URL: "http://localhost:3000"
          CRON_SECRET: "ci-placeholder"

  # DB-backed tests (currently the backup restore engine: balance arithmetic,
  # preview/commit parity, undo reversal). These need real Postgres semantics —
  # interactive transactions, FK constraints, BigInt balances — so they run
  # against a service container with migrations applied rather than a mock.
  integration-tests:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: ledgerly_test
        ports:
          - 5432:5432
        # Without this the first migrate can race the server's own startup.
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: "postgresql://ci:ci@localhost:5432/ledgerly_test"
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"

      - run: npm ci

      - run: npx prisma generate

      # deploy, not dev: apply the committed migrations exactly as production
      # would, so a missing migration fails here instead of in production.
      - name: Apply migrations
        run: npx prisma migrate deploy

      - name: Integration tests
        run: npm run test:integration
